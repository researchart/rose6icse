% Copyright Claudio Menghi, University of Luxembourg, 2018-2019, claudio.menghi@uni.lu  
function [ diGraph ] = createUnconnectedDiGraph( numOfNodes )
%createUnconnectedDiGraph Creates empty adjacency matrix to denote a graph.
% If diGraph( i,j ) is non-zero, there is an edge from i to j.
diGraph = zeros(numOfNodes);

end

